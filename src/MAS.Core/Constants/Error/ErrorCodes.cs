namespace MAS.Core.Constants.Error
{
    public enum ErrorCodes
    {
        BadRequest = 400,
        NotFound = 404,
        Unauthorized = 401,
        InternalServerError = 500,
        Else = 0
    }
}
